import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';

// basic boilerplate code needed for every single react app
ReactDOM.render(<App />, document.querySelector('#root'));
